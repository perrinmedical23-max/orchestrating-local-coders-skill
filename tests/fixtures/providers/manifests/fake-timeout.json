{
  "schema_version": 1,
  "provider_id": "fake-timeout",
  "provider_kind": "fixture",
  "command": "fake-timeout.sh",
  "capabilities": {
    "worktree": true,
    "writes_report": true,
    "streams_stdout": true,
    "supports_timeout": true
  },
  "description": "Fixture provider that exceeds the configured timeout."
}
